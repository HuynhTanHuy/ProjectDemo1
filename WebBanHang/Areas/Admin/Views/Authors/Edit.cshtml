@model WebBanHang.Models.Author

<div class="container mt-4">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0">
                        <i class="bi bi-pencil-square me-2"></i>Chỉnh sửa Tác giả
                    </h4>
                </div>
                <div class="card-body">
                    <form method="post" enctype="multipart/form-data">
                        <input asp-for="Id" hidden />
                        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                        
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label asp-for="Name" class="form-label">Tên tác giả <span class="text-danger">*</span></label>
                                <input asp-for="Name" class="form-control" />
                                <span asp-validation-for="Name" class="text-danger"></span>
                            </div>
                            
                            <div class="col-md-6 mb-3">
                                <label class="form-label">Ảnh đại diện</label>
                                @if (!string.IsNullOrEmpty(Model.PhotoUrl))
                                {
                                    <div class="mb-2">
                                        <img src="@Model.PhotoUrl" alt="@Model.Name" style="max-width: 100px; max-height: 100px; border-radius: 5px;" />
                                    </div>
                                }
                                <input type="file" name="file" class="form-control" accept="image/*" />
                                <small class="form-text text-muted">Chọn file ảnh mới để thay thế (tùy chọn)</small>
                            </div>
                        </div>
                        
                        <div class="mb-3">
                            <label asp-for="Biography" class="form-label">Tiểu sử</label>
                            <textarea asp-for="Biography" class="form-control" rows="5"></textarea>
                            <span asp-validation-for="Biography" class="text-danger"></span>
                        </div>
                        
                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-primary">
                                <i class="bi bi-check-circle me-1"></i>Cập nhật
                            </button>
                            <a asp-controller="Authors" asp-action="Index" class="btn btn-secondary">
                                <i class="bi bi-arrow-left me-1"></i>Quay lại
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts{
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}

