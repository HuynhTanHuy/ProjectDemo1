@model WebBanHang.Models.Author
@await Html.PartialAsync("_CustomerSubnav", "authors")

<div class="c-browse-page">
    <div class="c-toolbar mb-3" style="justify-content: flex-start;">
        <a class="c-btn-ghost" href="@Url.RouteUrl("customerAuthorBrowser", new { action = "Index" })">
            <i class="bi bi-arrow-left me-1"></i>Quay lại danh sách tác giả
        </a>
    </div>

    <header class="c-page-head c-page-head--row">
        @if (!string.IsNullOrEmpty(Model.PhotoUrl))
        {
            <img src="@Model.PhotoUrl" alt="" class="c-page-head__photo" />
        }
        <div>
            <h1 class="c-page-head__title">@Model.Name</h1>
            @if (!string.IsNullOrEmpty(Model.Biography))
            {
                <p class="c-page-head__sub mb-0">@Model.Biography</p>
            }
        </div>
    </header>

    <h2 class="c-section-title">Sách của tác giả</h2>
    <div class="row g-3">
        @if (Model.Products != null && Model.Products.Any())
        {
            foreach (var p in Model.Products)
            {
                <div class="col-6 col-md-4 col-lg-3">
                    @await Html.PartialAsync("~/Areas/Customer/Views/Product/_ProductCard.cshtml", p)
                </div>
            }
        }
        else
        {
            <div class="col-12 c-empty">Chưa có sách.</div>
        }
    </div>
</div>
